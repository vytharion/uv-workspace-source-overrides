[project]
name = "lib"
version = "0.9.0"
description = "Sibling fork of lib — same name, different greeting payload."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lib"]
