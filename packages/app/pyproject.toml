[project]
name = "app"
version = "0.1.0"
description = "Consumer package — will depend on lib starting in Lesson 2."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app"]
