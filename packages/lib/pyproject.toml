[project]
name = "lib"
version = "0.1.0"
description = "Producer package — exposes greet()."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lib"]
